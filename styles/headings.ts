import { StyleSheet } from "react-native";

const headings = StyleSheet.create({
    head18: {
        fontSize: 18,
        fontWeight: "bold"
    },
    head20: {
        fontSize: 20,
        fontWeight: "bold"
    },
    head24: {
        fontSize: 24,
        fontWeight: "bold"
    }
});

export default headings;